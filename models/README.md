# Trained models (later milestone)

No model is trained or supplied in this foundation.
Later, each method will have its own trained model and metadata linking it to the
dataset split, preprocessing parameters, feature version and class ordering.
Do not load untrusted pickle/joblib model files.
